import SwiftUI
import UIKit

struct parallelogram_fom_shapes: View {
    @Binding var fomulary_acti: Bool
    @Binding var fomulary_store: String
    var body: some View {
        List {
            HStack {
                Text("Sorry,but still working on it to find the character")
            }
            HStack {
                Text("Sorry,but still working on it to find the character")
            }
        }
    }
}
